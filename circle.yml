machine:
  environment:
    CODECLIMATE_REPO_TOKEN: 1b415a3f064b44dcefd71011b2d1208eca337c51017a7059b3c60cf31e21f026 codeclimate-test-reporter
  python:
    version: 2.7.10

checkout:
    post:
    - git submodule sync
    - git submodule update --init

dependencies:
  override:
    - pip install tox tox-pyenv codeclimate-test-reporter
    - pyenv local 2.7.10 3.5.0

#test:
#  override:
#    - make test