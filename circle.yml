machine:
  python:
    version: 2.7.10
  ruby:
    version: 2.1.3

dependencies:
  override:
    - gem install danger
    - pip install tox tox-pyenv codeclimate-test-reporter
    - pyenv local 2.7.10 3.5.0

test:
  override:
    - make ci