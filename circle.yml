machine:
  python:
    version: 2.7.10

checkout:
    post:
    - git submodule sync
    - git submodule update --init

dependencies:
  override:
    - pip install tox tox-pyenv
    - pyenv local 2.7.10 3.5.0

test:
  override:
    - make test